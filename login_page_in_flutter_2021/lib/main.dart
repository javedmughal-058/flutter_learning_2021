import 'package:flutter/material.dart';
import 'package:login_page_in_flutter_2021/login.dart';


void main() {
  runApp(SettingsUI());
}

class MyApp extends StatelessWidget {
  const MyApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
